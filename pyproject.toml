[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "smol-harness"
version = "0.1.0"
requires-python = ">=3.13"

[tool.setuptools]
py-modules = ["smolee"]

[project.scripts]
smolee = "smolee:main"
